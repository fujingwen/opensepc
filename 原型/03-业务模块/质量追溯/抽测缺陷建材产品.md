
# 抽测缺陷建材产品

抽测缺陷建材产品页面是质量追溯模块的一个子页面，当前已经有 `quality-trace` 方案、后端查询接口和主库表 `master.t_quality_trace`。

## 当前代码口径

- 功能：查询、导入、删除
- 主表：`master.t_quality_trace`
- 当前活动数据量：`616`
- 当前仍未收口的问题：
  - `batch_no` 为空 `616`
  - `product_name` 为空 `616`
  - “有无对比数据”仍依赖 `t_product_relation` 的弱关联判断

## 查询条件

当前实现的核心查询条件：

- 产品名称
- 生产批号
- 生产厂家

备注：

- 原始原型里提到的“检测项目”“检测日期”“有无对比数据”可作为后续增强项继续补，但本轮应以实际代码和接口为准。

## 列表字段

当前页面应以以下字段为准：

- 工程名称
- 检测项目
- 检验产品名称
- 生产厂家
- 生产批号
- 检测日期
- 报告日期
- 检验参数
- 结论
- 有无对比数据

## 数据说明

1. `master.t_quality_trace` 已是当前真实主表，不再按“待迁移到 master”描述。
2. 当前 `batch_no`、`product_name` 普遍为空，说明数据虽然已迁入，但页面口径和数据补录仍未收口。
3. “有无对比数据”依赖 `master.t_product_relation` 的弱关联：
   - `quality_trace_id` 当前仍为空
   - 主要依赖 `legacy_check_product_id`
