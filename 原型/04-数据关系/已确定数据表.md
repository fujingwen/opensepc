# 已确定数据表

## 主库已确认承载的核心表

以下表已经可以作为当前系统的真实落点，不再按“待确认”处理。

### 系统底座

- `master.sys_user`
- `master.sys_role`
- `master.sys_dept`
- `master.sys_post`
- `master.sys_menu`
- `master.sys_dict_type`
- `master.sys_dict_data`

### 基础业务

- `master.t_companyinfo`
- `master.base_province`
- `master.sys_product`

### 建材填报链路

- `master.t_project`
- `master.t_project_product`
- `master.t_record_product`

### 质量追溯链路

- `master.t_quality_trace`
- `master.t_product_relation`

### 消息与公告

- `master.base_message`
- `master.base_message_receive`
- `master.base_message_template`
- `master.msg_notice_publish`

## 保留但不再作为主承载口径的历史表

- `master.t_message`
- `master.mat_record`

说明：

- `t_message` 更接近历史企业消息语义，不再代表当前消息中心主链路。
- `mat_record` 不应继续扩展，备案产品主口径应回到 `t_record_product`。

## 当前仍需谨慎对待的表

- `master.t_product_relation`
- `master.base_message_template`

原因：

- `t_product_relation` 已落表，但与 `t_quality_trace` 的闭环仍弱。
- `base_message_template` 已存在，但当前开发库记录数为 `0`，暂时更像预留结构。
