# 企业信息问题记录

## 审查时间

- 2026-04-03

## 已完成事项

1. ~~生产企业页面分页、联系人、联系电话、地区中文名显示异常已修复。~~
2. ~~`master.t_companyinfo.create_by` / `update_by` 错误写入 `admin` 的问题已修复，现写入实际登录用户 ID。~~
3. ~~生产企业、施工企业、代理商页面权限标识已与数据库真实权限对齐。~~
4. ~~代理商新增页“所属生产企业”已由手工输入改为下拉选择，并复用生产企业列表。~~
5. ~~`getProductions` 接口已收敛为仅返回 `id` 和 `enterpriseName`。~~
6. ~~新增企业自动创建用户时的密码已改为加密落库。~~
7. ~~前后端联系电话校验已统一支持手机号和座机号。~~
8. ~~`del_flag is null` 的历史数据已清洗并兼容查询。~~

## 未完成问题

1. 历史 `area` 脏数据映射到 `base_province` 标准地区名称的兜底仍不足，部分旧数据无法稳定解析。

## 对照操作手册发现

1. 操作手册侧重“企业完善自身信息”与“生产单位创建本单位代理商”的自维护流程；当前实现仍提供施工企业、生产企业、代理商三类企业的后台主数据维护能力，范围大于原型定义。
2. 操作手册中的代理商维护应绑定当前生产单位上下文；当前实现仍允许从全量生产企业中选择所属企业，更接近后台管理模式。

## 验证记录

1. 已核对 `master.t_companyinfo`、`master.base_province`。
2. 已确认有效代理商记录存在，且父企业关联完整。
3. 已确认当前实现仍以 `base_province` 作为地区数据源。
