# 消息中心问题记录

## 审查时间

- 2026-04-03

## 已完成事项

1. ~~发送消息页面新增时“选择接收人报错”的前端异常已修复。~~
2. ~~消息查看页面因 `vnode` 为空导致的直接报错已修复。~~
3. ~~运行时消息表方向已收口为 `master.base_message`、`master.base_message_receive`、`master.base_message_template`，不再按 `master.msg_message` / `master.msg_message_user` 验收。~~
4. ~~顶部铃铛未读数已接入真实消息/公告摘要接口，不再依赖前端本地状态。~~
5. ~~业务类消息支持从统一提醒入口和详情页跳转到建材产品处理页。~~
6. ~~人工发送的消息已同步进入右上角统一提醒入口。~~
7. ~~消息已读 / 全部已读接口已调整为幂等成功返回，避免 0 行更新被前端误判失败。~~
8. ~~proposal/tasks 已回写统一提醒能力和当前实现状态。~~

## 未完成问题

1. 公告目前没有用户维度已读关系表，统一提醒中的公告暂按只读信息展示，不参与未读统计。
2. 历史迁移仍需兼容 `test.t_message` 与少量非纯数字用户标识，相关迁移闭环尚未完成。
3. 消息模板当前仅完成表结构与迁移预留，运行时模板发送能力尚未接入。

## 已核对结论

1. 人工发送消息当前使用主表 `base_message` 和收件表 `base_message_receive`。
2. 业务自动提醒直接写入 `base_message_receive`。
3. 顶部统一提醒当前通过 `/message/notification/summary` 汇总消息与已发布公告。
4. `test.t_message` 历史数据迁移时应并入新的人工消息模型，不再保留独立运行时表。
