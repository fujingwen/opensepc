# solution

问题1：删除改为逻辑删除，统一使用 `del_flag=2`

问题2、问题3：搜索问题先标记，不在本轮修改

问题4：`/system/region/treeselect` 当前不是必需接口
- 当前页面、树选择和组件都基于 `/list + parentId` 完成懒加载
- 本轮先不新增该接口
- 若后续出现“整棵树回显”或“树节点服务端搜索”场景，再补独立 `treeselect`

问题5：权限口径统一为“页面用 `list`，查询用 `query`”

问题6：补查 `RegionDialog`，并按 `common_region_dialog` 要求补齐编辑回显

问题7、问题8：本轮处理
- `RegionDialog` 编辑回显补齐
- 地区名称填充工具下沉为共享能力，并接入企业信息服务

问题9：按真实表结构修改实现，`type` 按字符串处理

问题10：先标记，不修改

问题11：只保留 `id='110228001'` 为活动数据，其余三条按逻辑删除处理；同时补主键、唯一索引和代码层严格唯一校验

问题12：删除标记统一使用 `del_flag`，`0=正常`、`2=删除`
