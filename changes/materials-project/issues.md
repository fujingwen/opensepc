# 工程项目问题记录

## 审查时间

- 2026-04-03

## 已完成事项

1. ~~工程项目页面与接口主体已切换到 `master.t_project`。~~
2. ~~一体化工程编码远程搜索、自动回填链路已具备。~~

## 未完成问题

1. `tasks.md` 仍停留旧口径，尚未回写当前实现与审查结论。
2. “施工单位”仍保留全量企业下拉选择，未按操作手册改为从当前登录施工单位自动带出。
3. 一体化工程编码筛选口径仍未完全统一，后端分页 SQL 仍按 `gc_code` 非空过滤，与设计要求按施工许可证口径不一致。
4. “信息确认单位”当前保存的是名称，不是标准字典编码，暂未形成稳定字典化方案。
5. `has_report` 口径与数据库实际值仍存在偏差，需要继续统一。
6. 历史 `construction_unit`、`is_integrated` 与施工许可证不一致的存量数据治理方案尚未明确。

## 对照操作手册发现

1. 操作手册要求“施工单位”基于当前登录身份自动带出，不允许人工重新选择；当前实现仍偏后台维护模式，与原型业务流程不一致。

## 验证记录

1. 已核对工程项目相关实现与 `master.t_project` 数据落点。
2. 字典迁移相关问题继续由独立提案 `dict-migration-test-to-master` 承接。
